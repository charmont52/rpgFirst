package command;

import org.newdawn.slick.command.Command;

/**
 * Enum class for the start page command
 */
public enum StartPageCommand implements Command {
    STARTGAME, OPTIONS, CREDITS, EXIT, NONE
}
